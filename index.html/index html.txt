<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rise and Rest - Login System</title>
  <link rel="manifest" href="manifest.json">
  <link rel="stylesheet" href="css/styles.css">
  <!-- Google Fonts & Material Icons -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet">
  <meta name="theme-color" content="#2563EB">
</head>
<body>

  <!-- Background App Blobs Grid -->
  <div class="glow-bg">
    <div class="blob blob-blue"></div>
    <div class="blob blob-green"></div>
  </div>

  <div class="auth-container">
    <!-- Header Brand -->
    <div class="brand-header">
      <div class="brand-logo">
        <span class="material-icons-round">wb_sunny</span>
      </div>
      <h1>Rise & Rest</h1>
      <p id="sub-school">Memperkuat Disiplin, Membangun Masa Depan</p>
    </div>

    <!-- Auth Card -->
    <div class="glass-card auth-card">
      <!-- Tabs Switcher -->
      <div class="tab-group">
        <button id="tab-siswa" class="tab-btn active" onclick="switchTab('siswa')">Siswa</button>
        <button id="tab-guru" class="tab-btn" onclick="switchTab('guru')">Guru Wali</button>
      </div>

      <!-- Form Login Siswa -->
      <form id="form-siswa" class="auth-form active" onsubmit="handleAuth(event, 'siswa')">
        <div class="input-group">
          <span class="material-icons-round input-icon">person</span>
          <input type="text" id="siswa-nama" placeholder="Nama Lengkap Sesuai Absen" required autocomplete="off">
        </div>
        
        <div class="form-row">
          <div class="input-group">
            <span class="material-icons-round input-icon">stairs</span>
            <select id="siswa-fase" required>
              <option value="" disabled selected>Pilih Fase</option>
              <option value="E">Fase E (Kelas X)</option>
              <option value="F">Fase F (Kelas XI/XII)</option>
            </select>
          </div>
          
          <div class="input-group">
            <span class="material-icons-round input-icon">class</span>
            <input type="text" id="siswa-kelas" placeholder="Contoh: X-A" required autocomplete="off">
          </div>
        </div>

        <button type="submit" class="btn btn-primary btn-block" id="btn-siswa-submit">
          <span>Masuk Program</span>
          <span class="material-icons-round">arrow_forward</span>
        </button>
      </form>

      <!-- Form Login Guru -->
      <form id="form-guru" class="auth-form" onsubmit="handleAuth(event, 'guru')">
        <div class="input-group">
          <span class="material-icons-round input-icon">supervisor_account</span>
          <input type="text" id="guru-nama" placeholder="Nama Lengkap Guru" required autocomplete="off">
        </div>
        
        <div class="input-group">
          <span class="material-icons-round input-icon">lock</span>
          <input type="password" id="guru-password" placeholder="Kata Sandi Akun" required>
        </div>

        <button type="submit" class="btn btn-secondary btn-block" id="btn-guru-submit">
          <span>Otentikasi Guru</span>
          <span class="material-icons-round">verified_user</span>
        </button>
      </form>
    </div>
  </div>

  <!-- Global Custom Toast Notification Element -->
  <div id="toast" class="toast-hidden"></div>

  <!-- Core Scripts Platform -->
  <script src="js/config.js"></script>
  <script src="js/auth.js"></script>
  <script src="js/app.js"></script>
</body>